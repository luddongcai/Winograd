#!/usr/bin/env python 
#coding:utf-8 

SchemaForm = ["pred(m,a)", "pred1(m,a)|pred2(m,b)"]

CausalWords = ["because", "so", "after", "before", "but", "that", "though", "although", "as", "when", "if","and", "or"]

